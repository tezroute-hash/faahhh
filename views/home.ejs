<% layout('layouts/boilerplate') -%>

<h2>All users</h2>
<div>
    <% for(let user of allUser) { %>
        <p>username : <%= user.username %></p>
        <p>email : <%= user.email%></p>
    <% } %>
</div>